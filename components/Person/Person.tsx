import React from 'react';
import { styles as classes } from './person.styles';

type PersonProps = {}

const Person = (props: PersonProps) => {
  return (
    <div>Person</div>
  )
}

export default Person;